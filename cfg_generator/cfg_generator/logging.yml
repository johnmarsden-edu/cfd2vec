appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} - [{h({l})}] (({t})) {m} (({M}, {f}, {L})){n}"

root:
  level: info
  appenders:
    - stdout

loggers:
  cfg_generator:
    level: debug